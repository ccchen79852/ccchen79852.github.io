spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/lin_blog?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD}
app:
  admin-password: ${ADMIN_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
server:
  port: 8080
