import {createApp,ref,onMounted} from 'vue'; import './style.css';
const App={setup(){const articles=ref([]),editing=ref(false),form=ref({title:'',slug:'',category:'随笔',excerpt:'',content:'',published:true});const load=()=>fetch('/api/articles').then(r=>r.json()).then(x=>articles.value=x);onMounted(load);const save=async()=>{await fetch('/api/admin/articles',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form.value)});editing.value=false;form.value={title:'',slug:'',category:'随笔',excerpt:'',content:'',published:true};load()};return{articles,editing,form,save}},template:`<header><b>拾光笔记</b><button @click="editing=!editing">{{editing?'返回文章':'写文章'}}</button></header><main v-if="!editing"><p>PERSONAL BLOG · 2026</p><h1>记录那些<br><i>发亮的日常</i></h1><article v-for="a in articles" :key="a.id"><small>{{a.category}} · {{new Date(a.createdAt).toLocaleDateString()}}</small><h2>{{a.title}}</h2><p>{{a.excerpt}}</p></article><p v-if="!articles.length">还没有文章，点击“写文章”发布第一篇吧。</p></main><main v-else class="editor"><h2>发布文章</h2><input v-model="form.title" placeholder="标题"><input v-model="form.slug" placeholder="链接标识，例如 first-post"><input v-model="form.category" placeholder="分类"><input v-model="form.excerpt" placeholder="摘要"><textarea v-model="form.content" placeholder="正文（支持纯文本）"></textarea><button @click="save">发布</button></main>`};createApp(App).mount('#app');
